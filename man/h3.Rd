% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-summary.R
\name{h3}
\alias{h3}
\title{3D Heatmap Plot (extension of heatmap3)}
\usage{
h3(
  x,
  Rowv = NULL,
  Colv = if (symm) "Rowv" else NULL,
  distfun = dist,
  distfunC,
  distfunR,
  balanceColor = F,
  ColSideLabs,
  RowSideLabs,
  showColDendro = T,
  showRowDendro = T,
  col = colorRampPalette(c("navy", "white", "firebrick3"))(1024),
  legendfun,
  method = "complete",
  ColAxisColors = 0,
  RowAxisColors = 0,
  hclustfun = hclust,
  reorderfun = function(d, w) reorder(d, w),
  add.expr,
  symm = FALSE,
  revC = identical(Colv, "Rowv"),
  scale = c("row", "column", "none"),
  na.rm = TRUE,
  ColSideFun,
  ColSideAnn,
  ColSideWidth = 0.4,
  ColSideCut,
  colorCell,
  highlightCell,
  file = "heatmap3.pdf",
  topN = NA,
  filterFun = sd,
  returnDistMatrix = FALSE,
  margins = c(5, 5),
  ColSideColors,
  RowSideColors,
  cexRow = 0.2 + 1/log10(nrow(x)),
  cexCol = 0.2 + 1/log10(ncol(x)),
  lasRow = 2,
  lasCol = 2,
  labRow = NULL,
  labCol = NULL,
  main = NULL,
  xlab = NULL,
  ylab = NULL,
  na.color = "grey",
  keep.dendro = FALSE,
  verbose = getOption("verbose"),
  useRaster = if (ncol(x) * nrow(x) >= 50000) TRUE else FALSE,
  ...
)
}
\arguments{
\item{x}{A numeric matrix containing the data to be plotted.}

\item{Rowv}{Specifies the row dendrogram or clustering (default is NULL).}

\item{Colv}{Specifies the column dendrogram or clustering (default is NULL).}

\item{distfun}{A function to calculate the distance matrix for rows (default is dist).}

\item{distfunC}{A function to calculate the distance matrix for columns (default is NULL).}

\item{distfunR}{A function to calculate the distance matrix for rows (default is NULL).}

\item{balanceColor}{Logical, indicating whether to balance colors (default is FALSE).}

\item{ColSideLabs}{Labels for the columns' side (default is NULL).}

\item{RowSideLabs}{Labels for the rows' side (default is NULL).}

\item{showColDendro}{Logical, indicating whether to show the column dendrogram (default is TRUE).}

\item{showRowDendro}{Logical, indicating whether to show the row dendrogram (default is TRUE).}

\item{col}{A color palette for the heatmap (default is a gradient from navy to firebrick3).}

\item{legendfun}{A custom function to create the legend (default is NULL).}

\item{method}{Clustering method passed to \code{hclustfun}. Default "complete".}

\item{ColAxisColors, RowAxisColors}{Axis label colors, passed through to \code{heatmap3::heatmap3}.}

\item{hclustfun}{Hierarchical clustering function. Default \code{stats::hclust}.}

\item{reorderfun}{Dendrogram-reordering function. Default \code{function(d, w) stats::reorder(d, w)}.}

\item{add.expr}{An expression to be evaluated after plotting, for adding extra graphical elements.}

\item{symm}{Logical, whether the input matrix is symmetric. Default FALSE.}

\item{revC}{Logical, whether to reverse the column order. Default \code{identical(Colv, "Rowv")}.}

\item{scale}{One of "row", "column", or "none": how to scale the matrix before plotting.}

\item{na.rm}{Logical, whether to remove NAs. Default TRUE.}

\item{ColSideFun, ColSideAnn, ColSideWidth, ColSideCut}{Column-side annotation controls, passed through to \code{heatmap3::heatmap3}.}

\item{colorCell, highlightCell}{Cell highlighting controls, passed through to \code{heatmap3::heatmap3}.}

\item{file}{Output PDF filename when \code{topN} is used. Default "heatmap3.pdf".}

\item{topN}{If set, restrict to the top N rows (by \code{filterFun}) and save to \code{file}.}

\item{filterFun}{Row-filtering function used with \code{topN}. Default \code{stats::sd}.}

\item{returnDistMatrix}{Logical, whether to return the computed distance matrix. Default FALSE.}

\item{margins}{A numeric vector of length 2, plot margins.}

\item{ColSideColors, RowSideColors}{Side-color annotation bars, passed through to \code{heatmap3::heatmap3}.}

\item{cexRow, cexCol}{Row/column label font size.}

\item{lasRow, lasCol}{Row/column label orientation.}

\item{labRow, labCol}{Row/column labels.}

\item{main, xlab, ylab}{Plot title and axis labels.}

\item{na.color}{Color used for NA cells. Default "grey".}

\item{keep.dendro}{Logical, whether to return the dendrogram. Default FALSE.}

\item{verbose}{Logical, verbosity. Default \code{getOption("verbose")}.}

\item{useRaster}{Logical, whether to use raster graphics for large matrices.}

\item{...}{Additional arguments passed to \code{heatmap3::heatmap3}.}
}
\description{
This function creates a 3D heatmap plot to visualize a matrix of data, extending \code{heatmap3::heatmap3}.
}
\details{
\itemize{
\item The \code{h3} function creates a 3D heatmap plot to visualize a matrix of data, extending and customizing \code{heatmap3::heatmap3} function.
\item It allows customization of various aspects of the plot, including colors, labels, and dendrograms.
\item Additional functionalities like distance matrix calculations and custom legends are available.
}
}
