% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell-selection.R
\name{subsetCells}
\alias{subsetCells}
\alias{subsetCells,CycifStack-method}
\title{Assemble a compact, analysis-ready table for a CellSelection}
\usage{
subsetCells(x, ...)

\S4method{subsetCells}{CycifStack}(x, selection, norm_type = c("log", "logTh"), ct_name = "default")
}
\arguments{
\item{x}{A CycifStack object. If it has a \code{cell_neighborhood} computed
(via \code{computeCN()}), neighbor_* columns are included; cells in
\code{selection} that aren't covered by that CellNeighborhood (e.g. outside
its \code{used.cts}) get NA in those columns, with a warning.}

\item{...}{Additional arguments (unused).}

\item{selection}{A CellSelection, from \code{computeSelection()}.}

\item{norm_type}{Which expression slot to use for center-cell expression: "log" or "logTh".}

\item{ct_name}{Which cell-typing to use (default "default").}
}
\value{
A CellFeatures object.
}
\description{
Pulls together center-cell expression, neighborhood composition/expression
(if \code{computeCN()} has been run), and clinical metadata for exactly the
cells named by \code{selection}, into one \code{CellFeatures} object -- a
thin wrapper around a single \code{data.table} plus the selection's
provenance. This is the one place row-alignment across those different
sources is handled; nothing downstream should need to re-derive it.
}
\seealso{
\code{\link{computeSelection}}
}
