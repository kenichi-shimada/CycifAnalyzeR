% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-spatial.R
\docType{class}
\name{CellNeighborhood-class}
\alias{CellNeighborhood-class}
\title{Class "CellNeighborhood" - Cell Neighborhood Object}
\description{
An object of class "CellNeighborhood" represents the results of the computeCN function
for a Cycif or CycifStack object, providing information about the cell neighborhood analysis.
}
\details{
Every per-cell slot below is stored \emph{compact}: only the cells for which
\code{is_used} is \code{TRUE} have a row. \code{is_used} itself is the one
full-length mask (length = \code{nCells(x)}, matching the parent Cycif/
CycifStack) that records which original cells those rows correspond to --
\code{which(is_used)} gives their original row indices. No slot is ever
padded with NA rows for unused cells; this is the same pattern
\code{LdRunUMAP()}/\code{LDCoords} already use for \code{is_used}/coordinates.
}
\section{Slots}{

\describe{
\item{\code{is_used}}{A logical vector, length = nCells(the source object). TRUE for
cells that were selected (within ROI, of a used cell type, has >=1
neighbor, and survived the n.sampling cap). All other slots below have
\code{sum(is_used)} rows, in the same order as \code{which(is_used)}.}

\item{\code{used.cts}}{A character vector of cell types considered in the neighborhood analysis.}

\item{\code{smpls}}{A character vector, length sum(is_used): which sample each stored row belongs to.}

\item{\code{neighbor_graph}}{A named list of \code{NN} objects, one per sample (neighbor
relationships are always sample-local, so there is no single cross-sample NN).}

\item{\code{n.neighbors}}{A numeric vector, length sum(is_used): number of neighbors (including self).}

\item{\code{mean_expr_per_ct_nbhd}}{A data.table, sum(is_used) rows: mean marker expression
among each cell's neighbors, grouped by neighbor cell type. Off-target
ab x celltype combinations (per the \code{off_target} argument to
\code{computeCN}) are NA, not silently averaged in.}

\item{\code{mean_expr_per_nbhd}}{A data.table, sum(is_used) rows: mean marker expression
pooled across all neighbors regardless of type. Uses the same off-target
masking as mean_expr_per_ct_nbhd.}

\item{\code{neighbor_counts}}{A matrix, sum(is_used) rows: counts of each neighboring cell type.}

\item{\code{neighbor_density}}{A matrix, sum(is_used) rows: neighbor_counts normalized by search area.}

\item{\code{neighbor_freq}}{A matrix, sum(is_used) rows: relative frequency of each neighboring cell type.}

\item{\code{dist_to_tumor_border}}{A numeric vector, sum(is_used) rows: distance of each cell to the tumor border.}

\item{\code{mclustda}}{A list; legacy slot used by the (currently unsupported) rcnClust()/tcnClust() path.}
}}

\seealso{
\code{\link{computeCN}}
}
