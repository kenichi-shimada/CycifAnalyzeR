% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell-selection.R
\name{selectionSummary}
\alias{selectionSummary}
\alias{selectionSummary,CycifStack-method}
\title{Report how many eligible cells are available per sample, before selecting any}
\usage{
selectionSummary(x, ...)

\S4method{selectionSummary}{CycifStack}(x, used.cts, ct_name = "default")
}
\arguments{
\item{x}{A CycifStack object.}

\item{...}{Additional arguments (unused).}

\item{used.cts}{A character vector of eligible cell types.}

\item{ct_name}{Which cell-typing to use (default "default").}
}
\value{
Invisibly, a list with \code{per_sample} (a data.frame of sample x
n_eligible) and \code{percentiles} (the 10th/20th/.../100th percentile of
those counts). Also prints both as a side effect.
}
\description{
A diagnostic step, meant to run before \code{computeSelection()}: reports how
many cells per sample match \code{used.cts}, and the percentile distribution of
those per-sample counts, so you can pick an \code{n_per_sample} value with the
actual numbers in front of you rather than guessing. Uses the exact same
eligibility filter \code{computeSelection()} will use, so the numbers here are
guaranteed to match what selection actually does.
}
\seealso{
\code{\link{computeSelection}}
}
